import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "localhost",
      "b.zmtcdn.com",
      "e-commerce-fe-eta-ruddy.vercel.app",
      "fastly.picsum.photos"
    ]
  },
};

export default nextConfig;
